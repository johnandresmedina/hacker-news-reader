import './CustomSpinner.scss';

import React from 'react';
import { Spinner } from 'reactstrap';

const CustomSpinner = () => <Spinner className='custom-spinner' type='grow' color='primary' />;

export default CustomSpinner;
